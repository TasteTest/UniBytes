namespace backend_loyalty.Common.Enums;

/// <summary>
/// Loyalty tier levels
/// Stored as integers in database
/// </summary>
public enum LoyaltyTier
{
    Bronze = 0,
    Silver = 1,
    Gold = 2,
    Platinum = 3
}
